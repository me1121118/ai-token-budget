[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ai-token-budget"
version = "0.1.0"
description = "Zero-dependency token counter, cost estimator, and spending cap guard for AI and LLM apps."
readme = "README.md"
license = "MIT"
authors = [{ name = "me1121118" }]
keywords = ["llm", "ai", "token-counter", "openai", "claude", "gemini", "cost-estimator"]
dependencies = []
